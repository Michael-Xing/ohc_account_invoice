# OHC账票生成服务环境变量配置示例（已合并、仅保留 MinIO 相关）
# 复制此文件为 .env 并根据需要修改配置

# 存储配置（固定为 MinIO）
STORAGE_TYPE=minio

# MinIO 配置（必填）
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=ohc-documents
MINIO_SECURE=false

# 应用配置
APP_NAME=OHC账票生成服务
APP_VERSION=1.0.0
APP_DESCRIPTION=OHC账票生成API服务
HOST=0.0.0.0
PORT=8000
DEBUG=false
RELOAD=false

# 模板配置
TEMPLATE_BASE_PATH=src/templates

# 文件名配置
FILENAME_INCLUDE_TIMESTAMP=true
FILENAME_MAX_LENGTH=200
